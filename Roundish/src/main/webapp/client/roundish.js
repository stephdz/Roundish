/**
 * Roundish module
 * @param window
 * @param angular
 * @param undefined
 */
(function(window, angular, undefined) {'use strict';

// Create the module
var module = angular.module('roundish', ['roundish-config','roundish-log','roundish-auth','roundish-rest']);

})(window, window.angular);